/* eslint-disable */

import { Element } from 'react-scroll';

import { ResumeContainer } from '../styled';

export default function Resume() {
  return (
    <Element name="resumo">
      <ResumeContainer>Resumo</ResumeContainer>
    </Element>
  );
}
